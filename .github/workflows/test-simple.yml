name: Test Simple Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
        run: |
          echo "✅ GitHub Actions is working!"
          echo "Current time: $(date)"
          echo "This workflow triggered successfully!"

