# Production credentials (Postgres on EC2) – copy to prod.env and edit with: nano backend/prod.env
# Deploy script will push prod.env to server as backend/.env

# Postgres on same EC2 (localhost)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/wissen_publication_group?schema=public"

# AWS S3 (optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=wissen-publication-group-files
CLOUDFRONT_URL=

NODE_ENV=production
PORT=3001
CORS_ORIGIN=*
